pid =

cert = stunnel-data/stunnel.pem

[https]
# accept=ACCESSING_PORT
# connect=WEBSERVER_PORT
# Basically, launch a development server on port indicated
# by `connect` and you can access HTTPS via port indicated
# by `accept`.
accept=1443
connect=1235
